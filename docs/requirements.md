Core Functional Requirements
The system must allow users to upload files
The system must store files in AWS S3
The system must trigger processing when a file is uploaded
The system must process files using AWS Lambda
The system must store processed output (e.g., thumbnails)
The system must allow users to retrieve processed results
The system must provide secure access to resources (via API Gateway/IAM)
